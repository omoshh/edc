apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ammonia-edc-edc-test
spec:
  type: ClusterIP 
  selector:
    app: edc-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000